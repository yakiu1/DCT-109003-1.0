import { TwoAtDirective } from './two-at.directive';

describe('TwoAtDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoAtDirective();
    expect(directive).toBeTruthy();
  });
});
